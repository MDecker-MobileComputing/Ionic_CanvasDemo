import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.canvasdemo',
  appName: 'CanvasDemo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
